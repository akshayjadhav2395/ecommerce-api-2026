server:
  port: 9092

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce_prod
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate

    show-sql: false